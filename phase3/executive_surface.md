# Executive Surface Contract

The executive reporting surface reads the same semantic metric IDs as the dashboard and ad-hoc analysis. It must not redefine formulas. For this demonstration, the executive surface is represented in the validation artifact by `same_metric_surfaces()` and shares the exact same metric values.
